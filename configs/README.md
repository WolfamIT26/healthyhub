# Configs / Cấu hình dùng chung

Thư mục này được chuẩn bị theo workspace structure của Prompt 14.

Hiện tại cấu hình runtime chính vẫn nằm ở:

- `config/`
- `.env.example`
- `.env.development.example`
- `.env.test.example`
- `.env.production.example`

Không lưu secret thật trong thư mục này.
