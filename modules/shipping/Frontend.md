# Shipping Frontend / Frontend Shipping

Customer Order screens display persisted `pending|shipped|delivered|cancelled|returned` labels. There is no tracking page, status editor or Admin Shipping UI in Prompt 33.1; browser state is never fulfillment authority.

