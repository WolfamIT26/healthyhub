# Category Backend / Backend Category

The Product public repository reads Category list/tree/detail from `categories` and `category_display_rules`. Tree construction uses persisted parent IDs; no frontend category fixture is authoritative.
