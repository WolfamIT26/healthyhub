# Category Decisions / Quyết định Category

Public navigation uses active/public Category plus active web display rule when present. Sort is display order, name and ID. A generated unique key enforces one active primary Category per Product.
